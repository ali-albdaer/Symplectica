// This file exists purely to make it clear that Three.js is loaded globally
// via a script tag in index.html. The runtime expects `THREE` to be defined
// on `window`.

// eslint-disable-next-line no-undef
window.THREE = window.THREE || THREE;
